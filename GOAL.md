# doc goal

## North star

让用户在自己控制的基础设施上，与人或 AI Agent 共同编辑一份文档，并能清楚回答：

1. 当前内容是什么；
2. 谁可以读取或修改；
3. 最近发生了什么变化；
4. 如何恢复到可信版本；
5. 如何完整迁移到另一套部署。

## Product promise

`doc` 提供一套可迁移的文档内核，供 UI、API、实时协作和 Agent 自动化共同使用。
部署者控制数据库、对象存储、模型凭据和认证边界。

## Acceptance scenarios

### 1. 创建与组织

用户可以创建嵌套文档、拖拽排序、搜索、收藏、移动到回收站并恢复。

### 2. 实时协作

两个获授权用户可以同时编辑同一文档；短暂离线后重新连接不会静默丢失已确认内容。

### 3. 可恢复版本

用户可以查看历史版本差异并恢复目标版本。恢复前，当前内容必须保存为新快照。

### 4. 可审阅 AI 修改

Agent 可以生成或改写内容，但写入正文前必须经过显式插入、替换或确认动作。

### 5. 自托管

新部署者只使用仓库文档、环境变量示例和容器依赖即可启动本地系统，不依赖特定品牌
域名或私有静态资源。

## Boundaries

`doc` 当前不承诺：

- 运行通用 Agent 工具循环或工作流；
- 取代完整 Office 套件；
- 为重要数据提供唯一备份；
- 在未配置权限时向 Agent 暴露全部文档；
- 把路线图能力描述为已交付能力。

## Relationship to mem

`mem` 保存跨 Agent 的原始资产、长期记忆与任务 checkpoint；`doc` 保存可编辑文档、
协作状态和版本。两者通过显式 API 连接，不共享隐藏数据库状态。
